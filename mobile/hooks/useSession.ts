// Hook — gestion de la session utilisateur
// TODO: créer la session au démarrage, persister le token dans SecureStore

export function useSession() {
  // TODO: 
  // - startSession() : appel POST /api/session/start, stocker token
  // - clearSession() : appel DELETE /api/session/clear, nettoyer SecureStore
  return {};
}
