import { View, Text, TouchableOpacity } from "react-native";

// Composant carte de question pour le questionnaire
// Props: question, onAnswer, onSkip
// TODO: implémenter l'affichage des choix selon le type (single, multiple, scale, text)
export default function QuestionCard() {
  return (
    <View>
      <Text>QuestionCard — TODO</Text>
    </View>
  );
}
