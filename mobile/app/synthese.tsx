import { View, Text } from "react-native";

// Écran de synthèse — résumé pédagogique des difficultés exprimées
// TODO: radar chart, observations par domaine, suggestions d'actions
// ⚠️ Toujours afficher le disclaimer : outil exploratoire, pas un diagnostic
export default function SyntheseScreen() {
  return (
    <View>
      <Text>Synthèse — TODO</Text>
    </View>
  );
}
