// Page synthèse — résumé pédagogique + orientation
// TODO: radar chart (Recharts), observations, suggestions, bouton corriger
// ⚠️ Disclaimer obligatoire : outil exploratoire, pas un diagnostic médical
export default function SynthesePage() {
  return <main><p>Synthèse — TODO</p></main>;
}
